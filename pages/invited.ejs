<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
  <meta name="description" content="Secure Scuttlebutt Pub Server"/>
  <title>Secure Scuttlebutt Pub Server</title>
  <style>
    html { background-color: #f8f9fa; }
    body {
      color: #212529;
      font-family: "Helvetica Neue", "Calibri Light", Roboto, sans-serif;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      letter-spacing: 0.02em;
      padding-top: 30px;
    }
    a { color: inherit; }
    footer {
      position: fixed;
      bottom: 30px;
      left: 0;
      right: 0;
      text-align: center;
      color: #adb5bd;
    }

    h1, p, svg { text-align: center; margin: 0 auto; }
    h1 { max-width: 15em; font-size: 1.3em; margin-bottom: 20px; }
    p { margin-bottom: 5px; max-width: 500px; }

    code {
      padding: 2px 3px;
      background-color: #e9ecef;
      border-radius: 3px;
      color: #343a40;
      font-size: 11px;
      font-weight: bold;
      overflow-x: scroll;
      max-width: 85vw;
      display: inline-block;
    }
  </style>
</head>
<body>

  <h1>Here is your personal invitation</h1>
  <p><code><%= invitation %></code></p>
  <svg
    width="180px" height="180px"
    viewBox="0 0 <%= qrSize %> <%= qrSize %>"
    style="display:none;"
    xmlns="http://www.w3.org/2000/svg" version="1.1"
    xmlns:xlink="http://www.w3.org/1999/xlink">
    <path d="<%= qrPath %>" />
  </svg>
  <footer>
    This server is <a href="#">open source</a> under the MIT license.
  </footer>

</body>
</html>