import assert = require('assert');

describe('easy-ssb-pub discovery peer', function () {
  it('should find and exchange invites with another peer', function (done) {
    assert.fail('', '', 'TODO', '');
  });

  it('should ignore peers with older versions', function (done) {
    assert.fail('', '', 'TODO', '');
  });

  it('should ignore peers with different discovery id prefix', function (done) {
    assert.fail('', '', 'TODO', '');
  });
});